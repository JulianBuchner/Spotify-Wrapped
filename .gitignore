# node
node_modules
dist

# env
.env
.env.*
!.env.production
!.env.example

# sqlite db + backups (incl. WAL journals)
*.db
*.db-shm
*.db-wal
backups/

# local one-off scripts (machine-specific paths)
backend/src/scripts/importStreamingHistory.ts

# personal Spotify data export (source for the history import)
my_spotify_data/

# logs
*.log

.claude